import type { Metadata } from 'next';
import DocPage from '../components/DocPage';

export const metadata: Metadata = { title: 'Оплата и безопасность — OFFmine' };

export default function PaymentSecurity() {
  return (
    <DocPage title='Оплата и безопасность'>
      <p>Появится позже</p>
    </DocPage>
  );
}
