{
  "_comment": "DO NOT EDIT: FILE AUTO GENERATED — offmine Next.js egg",
  "meta": {
    "version": "PTDL_v2",
    "update_url": null
  },
  "exported_at": "2026-04-10T00:00:00+00:00",
  "name": "offmine",
  "author": "admin@offmine.ru",
  "description": "offmine — Next.js 16 app powered by Bun",
  "features": [],
  "docker_images": {
    "Bun 1": "oven/bun:1"
  },
  "file_denylist": [],
  "startup": "cd /home/container && bun run start -- -p {{APP_PORT}}",
  "config": {
    "files": "{}",
    "startup": "{\"done\": \"Ready in\"}",
    "logs": "{}",
    "stop": "^C"
  },
  "scripts": {
    "installation": {
      "script": "#!/bin/bash\nset -e\n\napt-get update -qq && apt-get install -y --no-install-recommends git ca-certificates curl unzip\n\n# install bun\ncurl -fsSL https://bun.sh/install | bash\nexport BUN_INSTALL=\"/root/.bun\"\nexport PATH=\"$BUN_INSTALL/bin:$PATH\"\n\n# clone or update\nif [ -d /mnt/server/.git ]; then\n  git -C /mnt/server fetch origin\n  git -C /mnt/server reset --hard origin/${GIT_BRANCH}\nelse\n  git clone --branch ${GIT_BRANCH} https://github.com/BADtochka/offmine.git /mnt/server\nfi\n\ncd /mnt/server\nbun install --frozen-lockfile\nbun run build\n\n[ -d /mnt/server/.next ] || { echo '[egg] ERROR: .next dir missing after build'; exit 1; }\n\necho '[egg] install done'",
      "container": "debian:bookworm-slim",
      "entrypoint": "bash"
    }
  },
  "variables": [
    {
      "name": "Server Port",
      "description": "Port the Next.js app listens on. Must match the allocation in Pterodactyl.",
      "env_variable": "APP_PORT",
      "default_value": "3000",
      "user_viewable": true,
      "user_editable": false,
      "rules": "required|numeric|between:1024,65535",
      "field_type": "text"
    },
    {
      "name": "Git Branch",
      "description": "Branch to pull during reinstall",
      "env_variable": "GIT_BRANCH",
      "default_value": "main",
      "user_viewable": true,
      "user_editable": true,
      "rules": "required|string|max:64",
      "field_type": "text"
    }
  ]
}
